<p style="color: green"><%= notice %></p>

<%= render @beer %>

<div>
<%= link_to('Edit this beer', edit_beer_path) if current_user %> |
<%= link_to "Back to beers", beer_path %>
<%= button_to('Destroy this beer', @beer, method: :delete) if current_user %>
</div>
